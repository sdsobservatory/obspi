import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: '#144fe5',
    },
    secondary: {
      main: '#fde400',
    },
  },
});

export default theme;